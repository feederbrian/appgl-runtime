#include "Fp64Module.h"

namespace appgl::extensions::fp64 {

// GL entry-point routing remains in Core during Phase 1. Future phases will
// move df64-specific validation/binding decisions here after shader lowering
// and reflection metadata are live.

}  // namespace appgl::extensions::fp64
