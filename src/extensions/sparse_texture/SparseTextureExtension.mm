#include "SparseTextureModule.h"

namespace appgl::extensions::sparse_texture {

// Entry-point routing lands with the allocation migration. The module TU is
// present now so build wiring and ownership boundaries are stable before
// behavior moves out of GLContext.

}  // namespace appgl::extensions::sparse_texture

